# =============================================================================
# Sports Venue AI Chatbot — Python Dependencies
# =============================================================================
# Last verified: 2026-05-08
# Python: 3.10+
# Install: pip install -r requirements.txt
# =============================================================================

# --- Web Framework ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
python-multipart==0.0.9

# --- Database (PostgreSQL) ---
sqlalchemy[asyncio]==2.0.35
asyncpg==0.29.0
psycopg2-binary==2.9.12         # For direct PG access (DB creation, admin)

# --- Validation & Settings ---
pydantic==2.9.2
pydantic-settings==2.5.2

# --- Neo4j Knowledge Graph ---
neo4j==5.24.0

# --- Redis Cache ---
redis[hiredis]==5.1.1

# --- LangChain Core (AI Agent) ---
# IMPORTANT: These versions are tested together. Do not upgrade individually
# without verifying compatibility (langchain 0.3.x requires langchain-core <0.4).
langchain==0.3.1
langchain-core==0.3.63
langchain-text-splitters==0.3.8
langsmith==0.1.147

# --- LangChain LLM Providers ---
langchain-anthropic==0.2.3      # Claude API (optional, requires ANTHROPIC_API_KEY)
langchain-google-genai==2.1.5   # Gemini API (optional, requires GEMINI_API_KEY)
langchain-ollama==0.2.3         # Ollama local LLM (recommended, free)

# --- LangChain Community ---
langchain-community==0.3.1      # Community integrations
langchain-protocol==0.0.15      # Protocol support

# --- Vector Database ---
# chromadb==0.5.5               # Optional: for hybrid vector search

# --- Scraping & Data Collection ---
beautifulsoup4==4.12.3          # HTML parsing
httpx==0.27.2                   # Async HTTP client
requests==2.33.1                # Sync HTTP client
scrapy==2.11.2                  # Web scraping framework
yt-dlp==2024.8.6                # YouTube transcript extraction
pdfplumber==0.11.9              # PDF text extraction

# --- Environment ---
python-dotenv==1.0.1            # .env file loading

# --- Testing ---
pytest==8.3.3
pytest-asyncio==0.24.0

# --- Utilities ---
orjson==3.11.9                  # Fast JSON (used by langchain)
filetype==1.2.0                 # File type detection (used by langchain-google-genai)
